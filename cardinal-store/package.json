{
  "name": "cardinal-store",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "bunx tsc --noEmit",
    "lint": "cd .. && biome check cardinal-store --error-on-warnings",
    "lint:fix": "cd .. && biome check cardinal-store --write --unsafe --error-on-warnings",
    "test": "bun test"
  },
  "dependencies": {
    "cardinal-observability": "workspace:*",
    "bun-types": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.6.3"
  }
}
